function text(test) {
    var text = document.getElementById('exampleModalLabel')
    return text.innerText = '' + test
}

function textCadastro(txt) {
    var text = document.getElementById('exampleModalLabel')
    return text.innerText = '' + txt
}